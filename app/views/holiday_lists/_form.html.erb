<%= form_with(model: holiday_list, local: true) do |form| %>
  <% if holiday_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(holiday_list.errors.count, "error") %> prohibited this holiday_list from being saved:</h2>

      <ul>
      <% holiday_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
